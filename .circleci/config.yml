version: 1
jobs:
  build:
    branches:
      only:
        - master
    machine:
      node:
        version: latest
    steps:
      - add_ssh_keys:
          fingerprints:
            - "40:bf:aa:1e:1f:f6:03:62:24:d1:e3:d5:ac:cd:1b:6b"
      - checkout
      - run: cd my-app && npm install && npm install -g @angular/cli && ng build --prod --aot --progress=false
      - run: docker login -u $DOCKER_USER -p $DOCKER_PASS
      - run: docker image prune --all --force
      - run: docker build -t cmtoro0/carlos_nginx:latest .
      - run: docker push cmtoro0/carlos_nginx:latest
      - run: ssh -o StrictHostKeyChecking=no ubuntu@54.94.231.248 "/bin/bash /home/ubuntu/docker/deploy.sh"