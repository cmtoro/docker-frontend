version: 1
jobs:
  build:
    machine: true
    steps:
      - add_ssh_keys:
          fingerprints:
            - "40:bf:aa:1e:1f:f6:03:62:24:d1:e3:d5:ac:cd:1b:6b"
      - checkout
      - run: docker login -u $DOCKER_USER -p $DOCKER_PASS
      - run: docker image prune --all --force
      - run: docker build -t cmtoro0/carlos_nginx:latest .
      - run: docker push cmtoro0/carlos_nginx:latest
      - run: ssh -o StrictHostKeyChecking=no ubuntu@18.229.196.155 "/bin/bash /home/ubuntu/docker/deploy.sh"